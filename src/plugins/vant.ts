import { NavBar, Button, Cell, CellGroup, Icon, Row, Col, Field } from 'vant';
import Vue from 'vue';
Vue.use(Button)
  .use(CellGroup)
  .use(Cell)
  .use(NavBar)
  .use(Row)
  .use(Col)
  .use(Field)
  .use(Icon);
